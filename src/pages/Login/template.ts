export const template = `
  <main class="auth">
    <h1 class="auth__title">{{title}}</h1>
      <form class="auth-form">
        {{EmailInput}}
        {{PasswordInput}}

        {{Button}}

        {{Link}}
      </form>
  </main>
`;

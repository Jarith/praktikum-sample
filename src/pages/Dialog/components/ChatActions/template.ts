export const template = (PhotoInput: string, FileInput: string, LocationInput: string) => `
  <div class="hidden">
    <div>
      ${PhotoInput}
    </div>
    <div>
      ${FileInput}
    </div>
    <div>
      ${LocationInput}
    </div>
  </div>
`;
